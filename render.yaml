services:
  - type: web
    name: gamaex-api
    runtime: node
    region: oregon
    plan: free

    rootDir: .
    buildCommand: >
      npx pnpm@10 install --no-frozen-lockfile &&
      npx pnpm@10 --filter @gamaex/types build &&
      npx pnpm@10 --filter @gamaex/database db:generate &&
      npx pnpm@10 --filter @gamaex/api build
    startCommand: node apps/api/dist/main

    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: JWT_REFRESH_SECRET
        sync: false
      - key: ALLOWED_ORIGINS
        sync: false
      - key: WHATSAPP_VERIFY_TOKEN
        sync: false
